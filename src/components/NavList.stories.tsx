import { Meta } from "@storybook/react";
import NavList from "./NavList";

const meta: Meta<typeof NavList> = {
  title: "NavList",
  component: NavList,
  tags: ["autodocs"],
};

export default meta;